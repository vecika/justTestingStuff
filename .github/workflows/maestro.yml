name: Testing maestro

on:
  push:
    branches:
      - main

jobs:
  Echo_workflow:
    runs-on: [self-hosted]
    steps:
    - name: Contact
      run: echo Hello, world!
  Build_APK:
    needs: Echo_workflow
    runs-on: [self-hosted]
    steps:
    - name: Build APK name
      run: echo Build APK
  

    
